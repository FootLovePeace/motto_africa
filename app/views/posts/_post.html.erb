<div class="card">
  <%= link_to image_tag(post.image, class: :card__img ), "#"%>
  <div class="card__body">
    <%= link_to "#{post.title}", "#", class: :card__title%>
    <p class="card__summary">
      <%= "#{post.country.name}"%>
      <%= "#{post.genre.name}"%>
    </p>
  </div>
</div>